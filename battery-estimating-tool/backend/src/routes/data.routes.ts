import { Router } from "express"; 

const router = Router();

// router.post("/fetchLeaderboardData", fetchLeaderboardData)
// router.post("/fetchVisualizerData", fetchVisualizerData)

export default router;