{
  "name": "battery-estimating-tool",
  "version": "1.0.0",
  "description": "",
  "workspaces": [
    "client",
    "backend"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently -n CLIENT,SERVER -c blue,green \"npm run dev -w client\" \"npm run dev -w backend\"",
    "install:all": "npm install && cd client && npm install && cd ../backend && npm install",
    "evaluator:build": "docker build -t socapp-evaluator -f Dockerfile.evaluator .",
    "build": "npm run build -w client && npm run build -w backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.2.1",
    "drizzle-orm": "^0.45.1",
    "pg": "8.13.0",
    "scheduler": "^0.27.0"
  },
  "overrides": {
    "pg": "8.13.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
