{
  "name": "battery-estimating-tool",
  "version": "1.0.0",
  "description": "",
  "workspaces": [
    "client",
    "backend"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently -n CLIENT,SERVER -c blue,green \"npm run dev -w client\" \"npm run dev -w backend\"",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.2.1",
    "drizzle-orm": "^0.45.1",
    "pg": "^8.20.0",
    "scheduler": "^0.27.0"
  }
}
